# .scrutinizer.yml
filter:
    excluded_paths:
        - assets/*

build:
    environment:
        php:
            version: 5.4.41       # Common versions: 5.4, 5.5, 5.6 or hhvm

checks:
    php:
        code_rating: true
        duplication: true
